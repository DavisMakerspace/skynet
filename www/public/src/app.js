define([
    'libs/jquery',
    'libs/underscore',
    'libs/backbone',
], 

function($, _, Backbone)
{
    var app = {};
    app.ns = {};
    
    app.initialize = function() 
    {        
    }

    return app;
});
